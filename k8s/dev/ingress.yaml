apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chat-api-dev
  namespace: chat-api-dev
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: traefik
  rules:
    - host: chat-dev.leoferolive.com.br
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: chat-api-dev
                port:
                  number: 8000
